// Temporary config until auth module is ready.
// Replace CURRENT_USER_ID with the logged-in user's ID from JWT.
const API_BASE = 'http://localhost:5087';
const CURRENT_USER_ID = 'user-placeholder-id';
